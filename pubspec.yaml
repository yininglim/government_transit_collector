name: government_transit_collector
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^6.0.1
  flutter_local_notifications: 21.0.0
  flutter_map: ^8.3.1
  geolocator: ^14.0.2
  gtfs_realtime_bindings: ^1.2.0
  http: ^1.6.0
  latlong2: ^0.10.1
  path: ^1.9.1
  sqflite: ^2.4.2
  supabase_flutter: ^2.16.0
  timezone: ^0.11.0

dev_dependencies:
  fixnum: ^1.1.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  sqflite_common_ffi: ^2.3.6

flutter:
  uses-material-design: true
  assets:
    - .env
